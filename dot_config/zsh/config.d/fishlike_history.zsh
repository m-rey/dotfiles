# configure fish-like history search appearance
# see `CHARACTER HIGHLIGHTING` in `man zshzle` and
# https://github.com/zsh-users/zsh-history-substring-search#configuration for more info 

typeset -g HISTORY_SUBSTRING_SEARCH_HIGHLIGHT_NOT_FOUND='standout'
typeset -g HISTORY_SUBSTRING_SEARCH_HIGHLIGHT_FOUND='bold,standout'
